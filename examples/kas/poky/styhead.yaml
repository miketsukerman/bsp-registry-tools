# KAS configuration for Yocto Styhead (5.1)
# https://downloads.yoctoproject.org/releases/yocto/yocto-5.1/RELEASENOTES
header:
  version: 14

target: core-image-minimal

defaults:
  repos:
    branch: "styhead"
