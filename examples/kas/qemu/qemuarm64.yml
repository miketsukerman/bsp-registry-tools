# KAS machine configuration for QEMU ARM64 target
header:
  version: 14

distro: poky

machine: qemuarm64

target:
  - core-image-minimal
  - core-image-sato

local_conf_header:
  qemu: |
    DISTRO_FEATURES += "x11"
