# KAS machine configuration for QEMU x86-64 target
header:
  version: 14

distro: poky

machine: qemux86-64

target:
  - core-image-minimal
  - core-image-sato

local_conf_header:
  qemu: |
    DISTRO_FEATURES += "x11"
